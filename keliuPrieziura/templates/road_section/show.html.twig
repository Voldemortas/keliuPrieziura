{% extends 'base.html.twig' %}

{% block title %}RoadSection{% endblock %}

{% block body %}
    <h1>RoadSection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ road_section.id }}</td>
            </tr>
            <tr>
                <th>RoadNumber</th>
                <td>{{ road_section.roadNumber }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ road_section.name }}</td>
            </tr>
            <tr>
                <th>SectionStart</th>
                <td>{{ road_section.sectionStart }}</td>
            </tr>
            <tr>
                <th>SectionFinish</th>
                <td>{{ road_section.sectionFinish }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ road_section.level }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ road_section.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('road_section_index') }}">back to list</a>

    <a href="{{ path('road_section_edit', {'id': road_section.id}) }}">edit</a>

    {{ include('road_section/_delete_form.html.twig') }}
{% endblock %}
