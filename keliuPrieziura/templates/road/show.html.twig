{% extends 'base.html.twig' %}

{% block title %}Road{% endblock %}

{% block body %}
    <h1>Road</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ road.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ road.name }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ road.number }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ road.level }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('road_index') }}">back to list</a>

    <a href="{{ path('road_edit', {'id': road.id}) }}">edit</a>

    {{ include('road/_delete_form.html.twig') }}
{% endblock %}
