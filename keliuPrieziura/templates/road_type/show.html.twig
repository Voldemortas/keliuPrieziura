{% extends 'base.html.twig' %}

{% block title %}RoadType{% endblock %}

{% block body %}
    <h1>RoadType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ road_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ road_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('road_type_index') }}">back to list</a>

    <a href="{{ path('road_type_edit', {'id': road_type.id}) }}">edit</a>

    {{ include('road_type/_delete_form.html.twig') }}
{% endblock %}
