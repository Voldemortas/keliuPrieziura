<form method="post" action="{{ path('road_type_delete', {'id': road_type.id}) }}" onsubmit="return confirm('Ar tikrai norite ištrinti?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ road_type.id) }}">
    <button class="btn">Trinti</button>
</form>
