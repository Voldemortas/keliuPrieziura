<form method="post" action="{{ path('section_delete', {'id': section.id}) }}" onsubmit="return confirm('Ar tikrai norite ištrinti?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ section.id) }}">
    <button class="btn">Trinti</button>
</form>
