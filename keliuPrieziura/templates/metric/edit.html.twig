{% extends 'base.html.twig' %}

{% block title %}Edit Metric{% endblock %}

{% block body %}
    <h1>Edit Metric</h1>

    {{ include('metric/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('metric_index') }}">back to list</a>

    {{ include('metric/_delete_form.html.twig') }}
{% endblock %}
