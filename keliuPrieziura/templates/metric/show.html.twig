{% extends 'base.html.twig' %}

{% block title %}Metric{% endblock %}

{% block body %}
    <h1>Metric</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metric.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ metric.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('metric_index') }}">back to list</a>

    <a href="{{ path('metric_edit', {'id': metric.id}) }}">edit</a>

    {{ include('metric/_delete_form.html.twig') }}
{% endblock %}
