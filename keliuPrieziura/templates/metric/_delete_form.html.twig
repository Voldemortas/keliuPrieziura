<form method="post" action="{{ path('metric_delete', {'id': metric.id}) }}" onsubmit="return confirm('Ar tikrai norite ištrinti?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ metric.id) }}">
    <button class="btn">Trinti</button>
</form>
